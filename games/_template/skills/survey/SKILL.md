---
name: {{GAME_SLUG}}-survey
binding: survey
description: >
  {{GAME_NAME}} binding for the core survey skill: what a batch overview
  shows for this game and what makes an episode interesting.
---

# Survey binding — {{GAME_NAME}}

The core `survey` skill carries the process: aggregate, decompose, flag,
narrate — finding-first, with agent-written reasons. This binding supplies
what those words mean here.

## The overview table

*(the per-policy stats a batch overview shows for this game — typically a
subset of the ab binding's metrics plus game-color: what would a competitor
glance at first?)*

## Structure worth surfacing

*(who-beats-whom? role matchups? map effects? — the batch-level structure
this game rewards noticing)*

## What makes an episode interesting

*(the flags: blowouts, upsets, near-wins, degenerate behavior, taint
clusters — each with how to detect it from results/artifacts cheaply)*

## Tooling

*(any batch-survey tooling this binding ships, with its CLI documented)*
