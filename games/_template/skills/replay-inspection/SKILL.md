---
name: {{GAME_SLUG}}-replay-inspection
binding: replay-inspection
description: >
  {{GAME_NAME}} binding for the core replay-inspection skill: how to open,
  decode, and read this game's replays and the policy's own artifacts.
---

# Replay-inspection binding — {{GAME_NAME}}

The core `replay-inspection` skill carries the method: the replay is ground
truth, the policy's artifacts are its point of view, and diagnosis lives in
the gap between them. This binding supplies the game's formats and tools.

## Replays

*(format; how to open/watch one — viewer URL or local tooling; how to expand
or decode one into analyzable data if applicable; version-coupling warnings
if the decoder must match the game build)*

## Policy artifacts

*(what our policies' per-episode artifacts/logs contain; where the decision
trace lives; the shared clock — what field joins an artifact event to a
replay moment)*

## What to look at

*(for this game: what a diagnostic viewing session focuses on — the moments
that carry signal, e.g. deaths, votes, task abandonment, engagements)*

## Tooling

*(replay/artifact tooling this binding ships, with CLIs documented)*
