# Tentative lessons — session buffer (optimizer-wide)

This session's candidate lessons about running the optimizer itself —
game-agnostic only (game lessons go in the lab's own buffer). Buffer eagerly
and candidly; most entries will be noise, and that's fine. **Recurrence across
independent session buffers — not in-session conviction — is what graduates a
lesson** (via the `lessons-review` skill, into `best_practices.md`).

Lifecycle: this buffer is archived to `lessons_archive/` and re-minted by the
session-start hook, but only when it has new content — an untouched buffer is
left alone. If a session genuinely produced no lessons, write nothing.

One lesson per bullet. Include enough context that a future review pass can
tell whether a later session hit the same thing.

---

*(empty)*
